pre-commit:
  parallel: true
  commands:
    check:
      run: npm run check
    check:types:
      run: npm run check:types

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
